import React from "react";
interface ListConfig {
    targetDir: string;
    synthCommand: string;
}
export declare const List: ({ targetDir, synthCommand, }: ListConfig) => React.ReactElement;
export {};
