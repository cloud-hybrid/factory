import React from "react";
interface DiffConfig {
    targetDir: string;
    targetStack?: string;
    synthCommand: string;
}
export declare const CloudRunInfo: () => React.ReactElement;
export declare const Plan: () => React.ReactElement;
export declare const Diff: ({ targetDir, targetStack, synthCommand, }: DiffConfig) => React.ReactElement;
export {};
