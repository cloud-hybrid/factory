/// <reference types="react" />
export interface ResourceNameConfig {
    name: string;
    stackName?: string;
}
export declare const ResourceName: ({ name, stackName }: ResourceNameConfig) => JSX.Element;
