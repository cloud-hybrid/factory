import React from "react";
interface CommonSynthConfig {
    targetDir: string;
    targetStack: string;
    jsonOutput: boolean;
}
interface SynthConfig extends CommonSynthConfig {
    synthCommand: string;
}
export declare const Synth: ({ targetDir, targetStack, synthCommand, jsonOutput, }: SynthConfig) => React.ReactElement;
export {};
