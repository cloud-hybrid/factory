import React from "react";
import { DeployingResource, TerraformOutput } from "./models/terraform";
interface DeploySummaryConfig {
    resources: DeployingResource[];
}
export declare const DeploySummary: ({ resources, }: DeploySummaryConfig) => React.ReactElement;
interface OutputConfig {
    output: {
        [key: string]: TerraformOutput;
    };
}
export declare const Output: ({ output }: OutputConfig) => React.ReactElement;
export declare const Apply: () => React.ReactElement;
interface DeployConfig {
    targetDir: string;
    targetStack?: string;
    synthCommand: string;
    autoApprove: boolean;
}
export declare const Deploy: ({ targetDir, targetStack, synthCommand, autoApprove, }: DeployConfig) => React.ReactElement;
export {};
