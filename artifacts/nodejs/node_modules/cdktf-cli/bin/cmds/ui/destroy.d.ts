import React from "react";
import { DeployingResource } from "./models/terraform";
interface DeploySummaryConfig {
    resources: DeployingResource[];
}
export declare const DeploySummary: ({ resources, }: DeploySummaryConfig) => React.ReactElement;
export declare const DestroyComponent: () => React.ReactElement;
interface DestroyConfig {
    targetDir: string;
    targetStack?: string;
    synthCommand: string;
    autoApprove: boolean;
}
export declare const Destroy: ({ targetDir, targetStack, synthCommand, autoApprove, }: DestroyConfig) => React.ReactElement;
export {};
