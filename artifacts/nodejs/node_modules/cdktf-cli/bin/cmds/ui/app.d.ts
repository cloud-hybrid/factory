import React, { ReactNode } from "react";
interface TerraformErrorProps {
    children: ReactNode;
}
export declare const TerraformErrors: React.FunctionComponent<TerraformErrorProps>;
interface AppProps {
    children?: ReactNode;
}
export declare const App: React.FunctionComponent<AppProps>;
export {};
